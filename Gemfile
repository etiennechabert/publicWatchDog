source 'https://rubygems.org'

# Bundle edge Rails instead: gem 'rails', github: 'rails/rails'
gem 'rails', '4.2.0'
# Use mysql as the database for Active Record
gem 'mysql2'
# Use SCSS for stylesheets
gem 'sass-rails', '~> 5.0'
# Use Uglifier as compressor for JavaScript assets
gem 'uglifier', '>= 1.3.0'
# Use CoffeeScript for .coffee assets and views
gem 'coffee-rails', '~> 4.1.0'
# See https://github.com/sstephenson/execjs#readme for more supported runtimes
# gem 'therubyracer', platforms: :ruby

# Use jquery as the JavaScript library
gem 'jquery-rails'
gem 'jquery-ui-rails'
gem 'jquery-turbolinks'

# Turbolinks makes following links in your web application faster. Read more: https://github.com/rails/turbolinks
# gem 'turbolinks'
# Build JSON APIs with ease. Read more: https://github.com/rails/jbuilder
gem 'jbuilder', '~> 2.0'
# bundle exec rake doc:rails generates the API under doc/api.
gem 'sdoc', '~> 0.4.0', group: :doc
# JS Route
gem 'js-routes'
# Passing rails variables to javascripts
gem 'gon'

# Twitter bootstrap
gem 'therubyracer'
gem 'less-rails'
gem 'twitter-bootstrap-rails'
gem 'bootstrap_form'
gem 'bootstrap-select-rails'
gem 'font-awesome-rails'

# Rendering data in javascript
gem 'd3js-rails'
gem 'chart-js-rails'

# Improvement for distance_of_time_in_words
gem 'dotiw'

# Redis cache
gem 'redis'

# Http Request
gem 'httparty'

# Devise / Authentication of users
gem 'devise'

# Html Parser
gem 'nokogiri'

# Delayed_jobs
gem 'delayed_job_active_record'
gem 'daemons'

# Datatables
gem 'jquery-datatables-rails'

# Arel (Improve databases queries)
gem 'arel'

gem 'linefit'

group :production do
    gem 'airbrake'
end

group :development, :test do
  # Call 'byebug' anywhere in the code to stop execution and get a debugger console
  gem 'byebug'

  gem 'request_profiler'

  gem 'rubycritic'

  # Better errors gives a nice interface when execptions are throw
  gem 'better_errors'
  gem 'binding_of_caller'
  gem 'meta_request'

  # Access an IRB console on exception pages or by using <%= console %> in views
  gem 'web-console', '~> 2.0'

  # Spring speeds up development by keeping your application running in the background. Read more: https://github.com/rails/spring
  gem 'spring'

  gem 'factory_girl_rails'
end

group :test do
    gem 'rspec-rails'

    gem 'codeclimate-test-reporter', group: :test, require: nil
end

