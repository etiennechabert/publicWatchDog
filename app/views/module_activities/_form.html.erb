<%= form_for(@module_activity) do |f| %>
  <% if @module_activity.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@module_activity.errors.count, "error") %> prohibited this epitech_module_activity from being saved:</h2>

      <ul>
      <% @module_activity.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
