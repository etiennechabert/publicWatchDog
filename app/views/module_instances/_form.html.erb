<%= form_for(@module_instance) do |f| %>
  <% if @module_instance.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@module_instance.errors.count, "error") %> prohibited this epitech_module_instance from being saved:</h2>

      <ul>
      <% @module_instance.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
