<p id="notice"><%= notice %></p>

<h1>Listing Third Year Checkups</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @third_year_checkups.each do |third_year_checkup| %>
      <tr>
        <td><%= link_to 'Show', third_year_checkup %></td>
        <td><%= link_to 'Edit', edit_third_year_checkup_path(third_year_checkup) %></td>
        <td><%= link_to 'Destroy', third_year_checkup, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Third year checkup', new_third_year_checkup_path %>
